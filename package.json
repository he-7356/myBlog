{
    "scripts": {
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
"deploy":"GH=1 yarn docs:build && bash scripts/deploy-gh.sh"
  },
    "devDependencies": {
        "vuepress": "^1.5.1"
    }
}
